import React, { Component } from "react";
import { hot } from "react-hot-loader";

const Type = props => (
  <div id="dashbox">
    <h1> TYPE </h1>
    <p> Type Data... </p>
  </div>
);

export default Type;
